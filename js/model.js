class Model {  constructor() {  this.transactions = [];  }  addTransaction(type, category, amount) {  const newTransaction = {  id: Date.now(),  type: type,  category: category,  amount: parseFloat(amount),  };  this.transactions.push(newTransaction);  return newTransaction;  }  getTransactions() {  return this.transactions;  }  filterTransactions(type, category) {  let filteredTransactions = [...this.transactions];  if (type) {  filteredTransactions = filteredTransactions.filter(transaction => transaction.type === type);  }  if (category) {  filteredTransactions = filteredTransactions.filter(transaction => transaction.category === category);  }  return filteredTransactions;  }  deleteTransaction(id) {  this.transactions = this.transactions.filter(transaction => transaction.id !== id);  } }